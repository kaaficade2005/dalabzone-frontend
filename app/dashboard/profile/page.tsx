import ProfilePage from "@/app/(root)/profile/page";
import React from "react";

const page = () => {
    return (
        <div>
            <ProfilePage />
        </div>
    );
};

export default page;
